{{#each (range 0 repeatX) as |x|}}
  {{#each (range 0 repeatY) as |y|}}
    {{dag-rectangle click=(action clicked (array x y)) plot=plot gap=gap coordinates=(array x y) color="rgba(200, 200, 200, 0.2)"}}
  {{/each}}
{{/each}}
