import { coordinates } from 'ember-dag-generator/helpers/coordinates';
import { module, test } from 'qunit';

module('Unit | Helper | coordinates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coordinates([42]);
  assert.ok(result);
});
